ssm
===

Simple Stack Machine interpreter

See http://www.staff.science.uu.nl/~dijks106/SSM/


To build it run
```shell script
./gradlew assembleDist
```
This will produce a `.zip` and a `.tar` with the same contents.
